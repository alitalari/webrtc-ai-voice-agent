# Copy to `.env` (gitignored) for local development. NEVER commit real secrets.
# All provider keys are used server-side only — they must never reach the browser.

# ASR
DEEPGRAM_API_KEY=

# TTS
CARTESIA_API_KEY=

# LLM (default: Claude)
ANTHROPIC_API_KEY=
# Optional alternates
# OPENAI_API_KEY=
# GEMINI_API_KEY=

# Server
PORT=8080
