/**
 * Wire-protocol version. The client and server negotiate this on connect;
 * incompatible peers are rejected with a clear error (see docs/architecture.md).
 */
export const PROTOCOL_VERSION = '0.1.0';
