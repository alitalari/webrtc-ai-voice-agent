# coturn skeleton for the hosted demo. Hardened in Phase 5.
# Production uses short-lived (TTL) credentials, not static users.

listening-port=3478
fingerprint

# Use the REST/time-limited credential mechanism in production:
# use-auth-secret
# static-auth-secret=<set-via-env>
# realm=turn.example.com

# Abuse controls (tighten for the public demo):
# total-quota=100
# user-quota=6
# no-multicast-peers
